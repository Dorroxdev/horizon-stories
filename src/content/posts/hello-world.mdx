---
title: "Hello, world — proving the pipeline"
slug: "hello-world"
excerpt: "A stub post that exists to prove the MDX + frontmatter + Zod + resources cross-check pipeline works end-to-end. Will be deleted or replaced before Phase 02 merges to production."
pillar: "founder-stories"
publishedAt: "2026-05-18"
relatedResource: "saas-validation-toolkit"
authorBio: "Operator, product builder, founder of Horizon Launchpad."
draft: false
---

This stub post is here to validate that MDX loading, frontmatter parsing, Zod validation, and the cross-reference to `src/data/resources.ts` all work end-to-end.

It will be deleted or replaced before Phase 02 merges back to `production`. The seed post in Plan 02-05 is the real launch content.

## Why a stub matters

Without at least one MDX file, `import.meta.glob` returns an empty record and `loadPosts()` returns `[]`, which makes every downstream plan (`/posts` list, `/posts/:slug` detail, recent-posts widget) impossible to verify visually. Three more tasks would be working blind.

## What this post is NOT

It is not editorial content. It is not search-indexable yet (no `<Helmet>` until Plan 02-05). It is not styled for production. The `prose` theme integration happens in Plan 02-03 when the detail page lands.

## A second heading to test display font

Some body text with **bold**, *italic*, and an [inline link](https://example.com) so the prose theme is exercised. This paragraph deliberately includes a few extra sentences so the word count comfortably clears the fifty-word minimum that the new raw-MDX measurement enforces.

- List item one
- List item two with **bold inside**

1. First ordered item
2. Second ordered item

<Callout type="info">
  An info callout. Should have a teal-tinted left border.
</Callout>

<Callout type="warning">
  A warning callout. Red-tinted.
</Callout>

<Callout type="success">
  A success callout. Mint-tinted.
</Callout>

> A blockquote with a left border that should match the brand teal.

### A third-level heading

Final paragraph before the share buttons.
