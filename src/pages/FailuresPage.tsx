import ContentListing from '@/components/shared/ContentListing';

export default function FailuresPage() {
  return <ContentListing title="Failure Post-Mortems" subtitle="The stories nobody wants to tell — but everyone needs to hear. Real numbers, real mistakes, real lessons." filterCategory="failure" />;
}
